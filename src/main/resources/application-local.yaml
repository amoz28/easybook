spring:
  datasource:
    url: jdbc:mysql://localhost:3306/easybookdb
    username: root
    password:
#    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: localhost
    port: 1025
    username: hello
    password: hello

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

app:
  email: amoz4christ@gmail.com

springdoc:
  swagger-ui:
    path=/swagger-ui:
      html: /api/v1/auth/swagger